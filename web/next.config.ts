import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['avatars.githubusercontent.com', 'projetocapsuladotempo-production.up.railway.app'],
  },
}

export default nextConfig;
